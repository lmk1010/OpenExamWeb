# OpenExamWeb

OpenExam 官方下载站（纯前端 React + Vite）。

## 本地开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

产物在 `dist/`。下载链接默认指向 GitHub Releases `v0.2.2`。

## 域名

推荐使用 `openexam.online`（`.com` / `.io` 已被占用）。
